version: 0.2

phases:
  install:
    commands:
      - file stop-ec2.sh               # Debug info
      - dos2unix stop-ec2.sh          # Fix line endings
      - chmod +x stop-ec2.sh
  build:
    commands:
      - ./stop-ec2.sh notify
