version: 0.2

phases:
  install:
    commands:
      - chmod +x stop-ec2.sh        # Make the script executable
  build:
    commands:
      - ./stop-ec2.sh notify        # Run the script with 'notify' action
